@src/config.py 帮我写一个打印配置信息的test

@src/main.py 帮我分析和解释这个代码，每段代码做了什么，什么阶段运行了什么？（直接告诉我，不要修改代码）

@src/script_writer.py 帮我分析和解释这段代码，具体分析generate_script_auto_episodes函数，每个阶段做了什么（直接告诉我，不要修改代码）

帮我将以下任务添加到 feature_list.json 中：
1、将小说内容自主判断集数的范围调整到40-110
2、将阶段三 - 预处理：分割小说章节 和 映射每集到对应章节 的内容保存到日志，以便我分析是否有问题
3、将阶段三 - 循环分批：每批的系统提示词 和 用户提示词 的内容保存到日志，以便我分析，先暂停LLM生成，等我后续任务恢复LLM生成



















---------------------------------

整体流程图

  阶段一：决定集数
      ↓ 调用 LLM 分析小说复杂度
      ↓ 输出：【集数：80集】

  阶段二：生成逐集大纲
      ↓ 调用 LLM 为每集规划：核心剧情 + 结尾钩子 + 原文范围
      ↓ 输出：80集的完整大纲
      ↓ 解析为 {1: "第1集大纲...", 2: "第2集大纲...", ...}

  阶段三：分批生成正文
      ↓ 预处理：
          - 分割小说章节
          - 映射每集到对应章节
      ↓ 循环分批：
          - 第1批（1-10集）：提取大纲1-10 + 对应章节 → LLM生成 →     
  写入文件
          - 第2批（11-20集）：提取大纲11-20 + 对应章节 → LLM生成 →   
  追加文件
          - ...
          - 第8批（71-80集）：提取大纲71-80 + 对应章节 → LLM生成 →   
  追加文件
      ↓ 返回完整剧本